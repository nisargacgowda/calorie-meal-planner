exp1:
create folder, copy it's path
go to git bash and type : cd <path>
git init
ls -a
git log (if it says fatal u shld give username & email)
git config --global user.name nisarga
git config --global user.email "nisarga@gmail.com"
go to that folder and create a text file put some content into it
git add .
git commit -m "this is my first file"
git log
===================================================
exp2:
Do exp1 and continue 
git branch
git branch feature-branch
git checkout feature-branch
touch readme.txt report.xml index.html .gitignore error.log(enter)
go to any of the file and type something
git add .
git commit -m "msg"
git checkout master
after checkout edit the same file 
git add & commit
git merge feature-branch(conflict raised)
git mergetool
====================================================
exp3:
create folder, copy it's path
go to git bash and type : cd <path>
git init
go to GitHub ->create new repo->public->add readme on->create repo
copy that github url
in git bash:
git clone <github_url>
cd <repo-name>
touch main.py
add content into it,save
git add and commit
git push origin main
====================================================
exp4:
do exp3 and continue
Open someone else's repository
click fork->copy fork_url
git clone <your_fork_url>
cd <repo-name>
git checkout -b feature-1
touch feature.txt(view in desk only,add some content) 
git add and commit
git push origin feature-1
go to GitHub:
compare pull req->confirm pull req->merge pull req->confirm merge
come to gitbash:
git checkout main
git pull origin main
====================================================
exp5:
create one folder
git init
touch app.py(add some content into it)
git add & commit
git remote add origin <repo_url>
git push -u origin master
to create tag: git tag v1.0
to view tags: git tag
to create anotated tag: git tag -a v1.2 -m "v1.2 created"
git tag
to push tag: git push origin v1.0(to push specific)
git push --tags
to delete tag:
for local deletion: git tag -d v1.0
for remote deletion: git push origin -d v1.0
====================================================
exp6:
open cmd :mvn --version
mvn archetype:generate
num:num
choose num:num
groupid:com.bnmit
archifactid:sample-app
version-1.0:just press enter
package:com.bnmit
press Y
-------
cd sample-app
mvn package
mvn clean
mvn site
sample-app-->src-->main-->java-->com-->bnmit-->app.java-->open file and paste the Bank Application Code
mvn clean
mvn package
cd target
java -cp sample-app-1.0-SNAPSHOT.jar com.bnmit.BankService
=====================================================
exp7:
go to GitHub->create new repo->add text file named message.txt->add content->commit changes
go to Jenkins->give name->freestyle project
source code management->git->give url,add credentials ->change master to main
build steps->execute batch commands->
echo Jenkins Build Started
type message.txt
echo Build completed successfully > output.txt
post build actions->archive the artifacts->
output.txt
apply->save
go for build now->console o/p
====================================================
exp8:
go to GitHub->create new repo->add BankService.java file->add content->commit changes
go to Jenkins
name it->free style
discard old builds(5,5)
source code management->git->give url,add credentials
change branch specifier to main
triggers->poll scm (* * * * *)
build steps->execute windows batch command
javac BankService.java
java BankService
apply -> save
see output ->again go back to bankservice and edit something and build
compare both builds
-----------------
again configure->triggers->GitHub webhook & disable poll scm->save and apply
go to GitHub ->settings->add webhook->payload url(https://your-jenkins-instance.com)->content type(.json)->save
go to GitHub edit something and it will be automatically build if any changes done
===================================================
exp9 & exp10:
go to github->create new repo->Jenkinsfile(add)
login to Jenkins
new->pipeline->log rotation(5,5)
pipeline script with svm->git->give url and credentials->change master to main
apply and save 
====================================================
Exp 6,8:
public class BankService {
    public static void main(String[] args) {
        double balance = 20000.0;
        System.out.println("===== BANK APPLICATION =====");
        System.out.println("Initial Balance: " + balance);
        double deposit = 900.0;
        balance += deposit;
        System.out.println("Deposited: " + deposit);
        double withdraw = 300.0;
        if (withdraw <= balance) {
            balance -= withdraw;
            System.out.println("Withdrawn: " + withdraw);
        }
        System.out.println("Final Balance: " + balance);
        System.out.println("Bank operation completed successfully.");
    }
}
-----------------------------------
Exp9-10:
pipeline {
    agent any 
    stages {
        stage('Build') {
            steps {
                echo 'Building...'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing...'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying...'
            }
        }
    }
}





